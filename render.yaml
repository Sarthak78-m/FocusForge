services:
  # Backend: Spring Boot
  - type: web
    name: mindsprint-backend
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: JDBC_DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        value: https://mindsprint-frontend.onrender.com

  # Frontend: React (Vite)
  - type: web
    name: mindsprint-frontend
    env: static
    rootDir: frontend
    buildCommand: "rm -rf node_modules package-lock.json && npm install && chmod +x ./node_modules/.bin/vite && npm run build"
    staticPublishPath: "./dist"
    envVars:
      - key: VITE_API_BASE_URL
        value: https://mindsprint-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
