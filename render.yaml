services:
  # Backend: Spring Boot
  - type: web
    name: ai-study-coach-backend
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: JDBC_DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        value: https://ai-study-coach-frontend.onrender.com

  # Frontend: React (Vite)
  - type: static
    name: ai-study-coach-frontend
    plan: free
    rootDir: frontend
    buildCommand: "npm install && npm run build"
    staticPublishPath: "./dist"
    envVars:
      - key: VITE_API_BASE_URL
        value: https://ai-study-coach-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
